defmodule Etoile.TagManager do

  def add_label( username ) do
    IO.puts "Adding label"
  end
  def list_labels( username ) do
    IO.puts "Show labels"
  end
  def add_place( username ) do
    IO.puts "Adding place"
  end
  def list_places( username ) do
    IO.puts "Show places"
  end
end
